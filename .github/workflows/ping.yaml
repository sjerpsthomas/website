# .github/workflows/ping.yml
name: Ping Strapi

on:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl your API
        run: curl -s https://your-strapi-site.com/api/_health