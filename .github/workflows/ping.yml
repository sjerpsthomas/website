# .github/workflows/ping.yml
name: Ping Strapi

on:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl your API
        run: curl -s www.thomassjerps.nl/api/jh-portfolio-tags?locale=en